Place the smallest runnable test inputs here.

Default filenames expected by `example.env`:

- `target_protein.pdb`
- `target_ligand.mol2`

Optional:

- `target_fragments.sdf`

If you prefer different filenames or paths, edit `../example.env`.
